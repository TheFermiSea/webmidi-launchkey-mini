{
  "name": "web-synth-rust",
  "version": "1.0.0",
  "description": "An experimental synthesizer rewritten in Rust, using Web Audio / WebMIDI.",
  "main": "index.html",
  "directories": {
    "lib": "lib"
  },
  "scripts": {
    "start": "npm run build:wasm && npx http-server",
    "build:wasm": "npx wasm-pack build --target web --out-dir pkg rust-synth"
  },
  "license": "ISC",
  "devDependencies": {
    "http-server": "^14.1.1",
    "wasm-pack": "0.10.3"
  }
}
